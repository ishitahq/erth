# ── Core server ──────────────────────────────────────────────────────────────
fastapi==0.116.2
uvicorn[standard]==0.36.0
python-multipart==0.0.20
pydantic==2.12.2

# ── ML / Image processing ───────────────────────────────────────────────────
# CPU-only PyTorch — keeps Docker image ~400 MB instead of ~2 GB.
# HF Spaces free tier is CPU-only; ONNX handles fast inference anyway.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu

Pillow==11.1.0
numpy==2.2.6

# ── ONNX inference (primary CPU path for EfficientNet Stage 1) ───────────────
onnx==1.17.0
onnxruntime==1.22.0

# ── CLIP (Stage 2 — grade classification) ────────────────────────────────────
# Installed via Dockerfile: pip install git+https://github.com/openai/CLIP.git
# The package has no stable PyPI release, only GitHub.
# Weights (~338 MB) downloaded during Docker build by scripts/download_clip.py
# and stored at backend/models/ViT-B-32.pt
#
# To install locally:
#   pip install git+https://github.com/openai/CLIP.git
# Then run:
#   python scripts/download_clip.py

# ── Optional: Depth Anything V2 volumetric estimation (Stage 3) ──────────────
# Requires manual repo clone + checkpoint download. Not included in Docker.
# pip install opencv-python-headless==4.11.0.86
